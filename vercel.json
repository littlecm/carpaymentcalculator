{
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "commands": {
      "build": "next build && npm run build:widget"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
